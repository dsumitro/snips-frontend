import React, { Component } from "react";
import PropTypes from "prop-types";

export default class Account extends Component {
  static propTypes = {
    prop: PropTypes
  };

  render() {
    return <div>I guess the Account goes here</div>;
  }
}
